[project]
name = "ccproxy-custom-hooks"
version = "0.1.0"
description = "Custom hooks for ccproxy"
requires-python = ">=3.10"
dependencies = [
    "litellm",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
